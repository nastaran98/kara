{
  "name": "@kara/domain",
  "version": "0.0.0",
  "private": true,
  "description": "Kara's pure business logic (the Daily Engine, practice types). No framework imports — consumed as TypeScript source by whichever bundler pulls it in (Next.js, Metro, ...).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
